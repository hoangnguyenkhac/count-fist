[phases.setup]
nixPkgs = [
  "python3",
  "python3Packages.pip",
  "libGL",
  "glib",
  "pkg-config",
  "ffmpeg",
  "libjpeg",
  "zlib"
]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "gunicorn app:app"
